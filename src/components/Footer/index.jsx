import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss'

Footer.propTypes = {
  
};

function Footer(props) {
  return (
    <div>
      
    </div>
  );
}

export default Footer;